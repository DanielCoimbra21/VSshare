package Client.View;

import javax.swing.*;
import java.awt.*;

public class TopBar extends JPanel {
    private Dimension dimension = new Dimension(800, 50);

    public TopBar() {
        setLayout(null);
        setPreferredSize(dimension);
    }
}
